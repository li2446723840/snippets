{
  "Print to console": {
    // "scope": "javascript,typescript",
    "prefix": ["java", "request"],
    "body": [
      "let params = {};",
      "let { code, data, msg } = await $1(params);",
      "if (code === 0) {",
      "  console.log(data, \"data\");",
      "} else {",
      "  this.\\$message.error(msg);",
      "}"
    ],
    "description": "处理java请求"
  }
}
