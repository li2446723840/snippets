{
  "Print to console": {
    // "scope": "javascript,typescript",
    "prefix": ["ct", "dayin", "jishi"],
    "body": ["console.time(\"timerName\");", "console.timeEnd(\"timerName\");"],
    "description": "console.time(timerName) 启动一个计时器来跟踪某一个操作的占用时长。每一个计时器必须拥有唯一的名字，页面中最多能同时运行10,000个计时器。当以此计时器名字为参数调用 console.timeEnd(timerName) 时，浏览器将以毫秒为单位，输出对应计时器所经过的时间。timerName：新计时器的名字。 用来标记这个计时器，作为参数调用 console.timeEnd()可以停止计时并将经过的时间在终端中打印出来"
  }
}
